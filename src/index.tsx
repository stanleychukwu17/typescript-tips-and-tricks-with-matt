import React from 'react';
import ReactDOM from 'react-dom/client';
import {Component2} from './components/m1/App1';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);
root.render(
    <React.StrictMode>
        <Component2 />
    </React.StrictMode>
);